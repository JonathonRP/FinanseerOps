
import {default as Root} from './ScoreCard.svelte';
import {default as Header} from './ScoreCard-Header.svelte';
import {default as Content} from './ScoreCard-Content.svelte';
import {default as Label} from './Label.svelte';
import {default as Score} from './Score.svelte';

export const ScoreCard = {
    Root,
    Header,
    Content,
    Label,
    Score
}
